## Running the Platform (Docker Compose)

This project runs as multiple services (producer, processor, enricher) connected through Kafka.

### Prerequisites
* Docker Desktop

### Start everything
From the project root:

```bash
docker compose up --build